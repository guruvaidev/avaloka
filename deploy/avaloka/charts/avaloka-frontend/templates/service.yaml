apiVersion: v1
kind: Service
metadata:
  name: {{ include "avaloka-frontend.fullname" . }}
  labels:
    {{- include "avaloka-frontend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "avaloka-frontend.selectorLabels" . | nindent 4 }}
